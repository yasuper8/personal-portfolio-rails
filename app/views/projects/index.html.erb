<div>
  <h1>All Projects</h1>
  <h2>There are <%= @projects.count %> projects registered</h2>
  <% @projects.each do |project| %>
    <%= render partial: 'partials/project', locals: {project: project} %>
  <% end %>
</div>
